{
  "name": "lead-gen-plus",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run clean && prisma generate && next dev -H 127.0.0.1 -p 3000",
    "prebuild": "npm run verify:ui-types && prisma generate",
    "build": "next build",
    "prestart": "prisma generate",
    "start": "next start -H 127.0.0.1 -p 3000",
    "lint": "next lint",
    "prisma:generate": "prisma generate",
    "clean": "node -e \"try{require('fs').rmSync('.next',{recursive:true,force:true})}catch(e){}\"",
    "postinstall": "prisma generate",
    "verify:ui-types": "node scripts/verify-no-prisma-client-in-ui.js"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "googleapis": "^144.0.0",
    "next": "15.5.12",
    "next-auth": "^4.24.8",
    "react": "18.3.1",
    "react-dom": "18.3.1",
    "resend": "^4.0.1"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "autoprefixer": "^10.4.20",
    "postcss": "^8.4.49",
    "prisma": "^5.22.0",
    "tailwindcss": "^3.4.16",
    "typescript": "^5.6.3"
  }
}
